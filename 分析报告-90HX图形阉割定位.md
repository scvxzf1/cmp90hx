# CMP 90HX 相对 GA102 消费卡的图形阉割分析报告

> 生成方式：`others/compare.py` 比对 5 张卡 BAR0 dump（90HX 解锁算力后 + 3×RTX3090 + 1×RTX3080Ti）
> 数据源：`others/*.txt`（每张约 375 万行，4M 个 dword）
> 参考项目：`/home/aubrey/0/cmpunlocker-amogh`（GA100/170HX 解锁方法论，同套路不同 die）

## 一、结论先行

90HX 游戏性能极差的原因**不是某几个寄存器被改值**，而是 **整个 PGRAPH（3D/图形）引擎的 PRI 私有空间被整体门控（readback = `0xBADFxxxx`）**，根本原因是：

> **90HX 跑的是 compute-only GSP-RM 固件（`gsp_tu10x.bin` 的算力变体），它在引导时跳过了图形引擎的初始化序列。** 主项目 patch 只解锁了算力相关寄存器，完全没碰图形管线（已 grep 确认 patch 内零个 PGRAPH/FECS/GPCCS/PMC_ENABLE 引用），所以图形引擎停留在「子引擎未上电 / PRI 门控」状态。

这跟 170HX 的「fuse 限制算力」是**完全不同的两类问题**：
- 170HX（GA100）：算力被 speed-select fuse 节流 → 写 SS0/SS1/CFG1 即恢复
- 90HX（GA102）：算力 OK，但**图形引擎整个没初始化** → 需要让它跑图形 GSP 初始化

## 二、铁证数据

### 2.1 图形门控候选按区段分布（17456 个候选地址）

| BAR0 区段 | 候选数 | 占比 | 含义 |
|---|---|---|---|
| **PGRAPH2 (GPC/TPC/ZROP/CROP/3D)** | **7177** | **41%** | 3D/光栅化引擎私有空间，90HX=BADF，消费卡=真值 |
| **FIFO/CE (主机图形命令提交)** | **5538** | **32%** | 图形通道/FIFO，90HX=absent，消费卡=真值 |
| FBHUB/MMU | 2865 | 16% | LTC/分区，部分是 SKU 差异（见 §3） |
| 其它 | 1876 | 11% | — |

### 2.2 门控性质区分（关键）

| 区段 | 90HX=BADF（引擎门控） | 90HX=absent（未初始化） |
|---|---|---|
| PGRAPH2 | **7026** | 148 |
| FIFO/CE | 0 | **4680** |

→ **PGRAPH2 是「引擎被门控」**（PRI 返回 BADF = 子引擎 power-gated / 时钟未开）；
→ **FIFO/CE 是「从未初始化」**（寄存器存在但 GSP 没写值，全 0）。
两者都指向同一根因：**GSP-RM 没跑图形 init**。

### 2.3 PGRAPH2 子引擎门控分布（按 64KB 前缀）

| 地址前缀 | 候选数 | 子引擎 |
|---|---|---|
| 0x520000 / 0x530000 | 1471 / 1307 | **TPC（纹理/像素处理集群）** |
| 0x670000 | 1280 | **GPC7 私有（光栅化）** |
| 0x690000 | 712 | GPC 私有 |
| 0x510000 | 314 | per-GPC SM |
| 0x680000 / 0x620000 | 538 / 445 | GPC 私有 |
| 0x630000–0x640000 | 72/95 | GPC 私有 |

**TPC 和 GPC 私有空间几乎全片 BADF** = 整个光栅化/像素管线没上电。

### 2.4 顶层图形功能标志位

```
FEAT_READOUT_0 @ 0x00823814
  90HX  = 0x00000033   ← bit8 CLEAR
  3090  = 0x00000233   ← bit8 SET
```
**bit8 = graphics/3D-engine-functional 标志**。90HX 这个顶层 readout 就缺图形位，
说明 GSP-RM 在内部把这张卡分类成「无图形引擎」SKU，于是根本不分配/初始化 PGRAPH。

### 2.5 顶层 PGRAPH 配置寄存器却在（反证：硬件 PGRAPH 存在）

```
0x00500000  90HX=0x8700029F  3090=0x8700029F   ← PGRAPH 顶层配置完全相同
0x00500004  90HX=0x08000000  3090=0x08000000   ← 同
```
**硬件 PGRAPH 引擎在 90HX 上物理存在且能响应顶层配置**——只是 GSP-RM 没初始化它的子引擎。
这证明：**不是 fuse/硬件阉割，是固件层面的 init 跳过**。这是好消息：理论上可恢复。

## 三、SKU 级硬件差异（无法解锁，解释性能上限）

这部分是**真实硅片差异**（floor-sweep fuse），与图形阉割无关，90HX 物理上比 3090 小：

| 寄存器 | 90HX | 3090共识 | 说明 |
|---|---|---|---|
| `FUSE_GPC_DISABLE` @0x820350 | 0x03 | (无fuse) | 90HX 多禁用 GPC bit0,1（少 2 个 GPC） |
| `FUSE_FBP_DISABLE` @0x820364 | 0x08 | (无) | 禁用 FBP bit3 |
| `FUSE_FBIO_DISABLE` @0x82036C | 0x08 | (无) | 禁用 FBIO bit3 |
| `FBPA_NUM_ACTIVE` @0x9A0164 | 5 | 6 | 少 1 个 FBP 分区 |
| `MMU_NUM_ACTIVE_LTCS` @0x100EC0 | 10 LTC | 12 LTC | 少 2 个 L2 缓存切片 |
| `FUSE_PCIE_DEVIDA` @0x8204D8 | 0x220D | 0x2204 | 设备 ID |
| `FUSE_PCIE_MAGIC_D` @0x820520 | 0x16E80000 | 0x10A80000 | SKU 标识 |

→ **即使图形引擎恢复，90HX 也会比 3090 慢 ~15-20%**（少 GPC/LTC），但绝不该是「极差」级别。

## 四、已排除的次要项

- **speed select 节流**：`FUSE_SS_FMLA32/IMLA4` 90HX=0x5 vs 3090=0x1，但这些是**算力** throttle，主项目已解锁；dump 里的残留值是 fuse OTP 原始位，effective 值（`FEAT_OVR_SM_SPD`）已被 patch 改写。**与图形无关**。
- `SM_ISSUE_RATE_MOD` @0x504204：90HX=0x7 vs 3090=0x5。这是 SM 调度节流，是算力域，非图形。
- `SKED_UNK54` @0x407054：仅 3090a 有 0x60000600，其它卡都 0/absent——是 per-unit 噪声，非阉割。

## 五、恢复图形性能的可行路径（按可行性排序）

### 路径 A（最可行）：让 GSP-RM 跑图形初始化 ⭐⭐⭐

根因是固件跳过 init。两种子方案：

1. **换用消费卡 GSP 固件**：把 90HX 的 GSP-RM 固件换成同 die（GA102）的 RTX 3090/3080 固件，让 GSP 以为这是消费卡从而初始化 PGRAPH。**风险**：固件签名校验（BootROM error 0x35，见 170HX 教训），需要先打开 PLM。170HX 整固件替换已被 BootROM 拒绝，90HX 可能同样拒绝。
2. **patch GSP-RM 的 SKU 判定**：定位 GSP 固件里读 `FEAT_READOUT_0 bit8` 或设备 ID 后分支跳过图形 init 的代码，NOP 掉那个跳转。这是 610 patch 套路的图形版。**前提**：需要逆向 GSP-RM 固件（RISC-V），工作量大但 170HX 社区已有工具链。

### 路径 B：手动初始化 PGRAPH（驱动源码层）⭐⭐

仿照 170HX 610 patch 的思路，在驱动 `kgspBootstrap` 之后、或在 RM 里**手动写 PGRAPH 子引擎的 init 序列**（即消费卡 dump 里那些 BADF→真值 的寄存器）。
- **难度**：PGRAPH2 区 7177 个寄存器，且很多是 per-GPC/per-TPC 多播，手动写值极易出错或漏步骤。
- **更现实**：只恢复 FIFO/CE（主机命令提交，4680 个 absent 寄存器）+ 顶层 PGRAPH，让驱动至少能提交图形命令；子引擎让 GSP 自己补。

### 路径 C（不可行）：fuse 层恢复 ❌
`FUSE_GPC_DISABLE` 等是 OTP fuse，物理烧断，无法恢复（这部分是真实硬件阉割，非门控）。

## 六、给后续工作的具体下一步

1. **确认 GSP 固件差异**：对比 90HX 出厂 GSP 固件 vs RTX3090 GSP 固件（`/lib/firmware/nvidia/gsp_tu10x.bin`），找图形 init 跳转点。
2. **抓 90HX 在 GSP 引导期间的 dmesg**：看是否有「skipping graphics init」类日志或 Xid。
3. **最小可行实验**：用 170HX 的 PLM-open 套路（patch 已验证能开 `FEAT @0x823804` PLM），尝试在驱动加载后手动写 `0x823814` 的 bit8，看 GSP 是否重新评估图形引擎存在性。
4. **优先验证路径 B 的子集**：先只恢复 FIFO/CE 区（0x340000-0x3C0000）的 absent 寄存器，跑一个简单 OpenGL/Vulkan 测试看是否有任何画面输出。

## 附：脚本与数据产物

- `others/compare.py` — 重写后的对比脚本（numpy 向量化，7 秒跑完 5 卡）
- `others/compare_candidates.csv` — 全量候选寄存器（GFX_DISABLE / VALUE_DIFF / CMP_ONLY 三类）
- 运行：`cd others && python3 compare.py`
